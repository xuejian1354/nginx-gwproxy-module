http {
    server {
        listen       80;

        location /* {
            http_proxy on;
        }
    }
}

stream {
    server {
        listen 1080;
        socks_proxy on;
    }

    server {
        listen 1081;
        gw_proxy on;
    }
}
